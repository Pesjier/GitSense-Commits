# Activity Summary for 2/9/2026

## 11:21:50 AM
The `UDPsender.py` file details the development of a Python script for sending UDP packets.

**File-Specific Updates (`UDPsender.py`):**

*   **Initialization (2/9/2026, 10:21:37 AM):** The script was initially set up to send UDP datagrams to `193.11.185.41:12000`. It defined a `firstPackageNum` starting at `10001` and an `Endofmsg` marker `'####'`. The script prompted the user for a message, prepended the package number, appended the `Endofmsg`, and sent it, incrementing `firstPackageNum` and sleeping for 1 second (`rate=1`, `time=1000/rate`).
*   **Input Validation & Rate Unit Change (2/9/2026, 10:23:36 AM - 10:26:50 AM):**
    *   An `if` condition was introduced to check the length of the user-input message. Initially, it was `if len(msg.encode()) < 1456: break` (10:23:36 AM), which would exit if the message was too short.
    *   This condition was quickly corrected to `if len(msg.encode()) > 1456: break` (10:24:07 AM), exiting if the message exceeded 1456 bytes.
    *   The calculation for `time` was changed from `1000/rate` (milliseconds) to `1/rate` (seconds) (10:26:50 AM), significantly altering the sleep duration.
*   **Automated Sending & Rate Increase (2/9/2026, 10:44:02 AM - 10:46:36 AM):**
    *   The user input prompt and associated length check were commented out.
    *   A fixed message `msg = 'yo'` was introduced for continuous sending.
    *   The `rate` was increased from `1` to `100` (10:44:02 AM) and then adjusted to `30` (10:46:36 AM), making the sending much faster.
*   **Minor Refactoring (2/9/2026, 10:47:34 AM):** A new variable `out_msg` was introduced to hold the formatted message string before encoding and sending, improving readability.
*   **Conditional Logic & Bug Introduction (2/9/2026, 10:50:06 AM - 10:52:34 AM):**
    *   An `x` counter and a conditional block (`if x==5:` then `if x==13:`) were added, seemingly to introduce some variable behavior for `firstPackageNum`.
    *   Due to incorrect indentation and subsequent commenting, this phase introduced bugs where `firstPackageNum` increments and `sleep(time)` became conditional or effectively non-functional.
*   **Loop Termination & Bug Resolution (2/9/2026, 10:57:56 AM):**
    *   The infinite `while True:` loop was replaced with `while sentPackages < 1500:`, limiting the total number of packets sent.
    *   A `sentPackages` counter was added and incremented.
    *   Crucially, `firstPackageNum+=1` and `sleep(time)` were correctly placed outside any conditional or commented block, ensuring they executed for every packet, resolving previous logical issues.
*   **Rate Tuning (2/9/2026, 11:01:19 AM - 11:20:37 AM):** The sending `rate` was iteratively adjusted, increasing from `30` to `50` (11:01:19 AM), then `55` (11:18:50 AM), and finally `65` (11:20:37 AM) packets per second. A minor typo in a comment was also fixed (`#hell` to `#hell0`).

**Patterns and Recurring Elements:**

*   **UDP Protocol:** The script consistently uses `socket(AF_INET, SOCK_DGRAM)` to establish a UDP sender, indicating its core purpose is non-connection-oriented data transmission.
*   **Fixed Destination:** The `serverport` (`12000`) and `serverIP` (`193.11.185.41`) remain constant throughout all changes.
*   **Packet Sequencing:** A `firstPackageNum` (initialized to `10001`) is consistently prepended to each message, followed by a semicolon, and then an `Endofmsg` marker (`####`), suggesting a simple form of packet ordering or identification. This `firstPackageNum` is incrementally updated.
*   **Rate-Limited Sending:** The `rate` variable and `sleep(time)` function are consistently used to control the sending frequency, with the `time` calculation being `1/rate` in seconds after the initial change. This suggests an ongoing effort to test different sending speeds.
*   **Iterative Development:** The logs show a clear pattern of incremental changes, often commenting out old code for new functionality (like user input vs. fixed message) and rapidly adjusting parameters like `rate`, indicating an iterative development and testing process. Errors in logic (like the `x` counter bug) were introduced and then resolved in subsequent commits.